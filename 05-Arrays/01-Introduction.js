// An array in javaScript is a data structure that stores an ordered list of elements. 
// it can hold elements of any data type, including numbers, strings, objects, and even other arrays. 
// Arrays are a type of object in javascript and have a number of built-in methods for adding, removing, and munipulating elements.
// Creating an array
let arr = [10, 20, 30, 40, 50];
console.log("Array:", arr);

let arr1 = new Array(10, 20, 30, 40, 50);
console.log("Array using constructor:", arr1);

let arr2 = new Array("Yash", "John", "Alice");
console.log("Array of strings:", arr2);

let arr2D = [[1, 2], [3, 4], [5, 6]];
console.log("2D Array:", arr2D);

let arr3 = new Array(5); 
arr3 = [0, 1, 2, 3, 4];
// Creates an array of length 5 with all elements undefined
console.log("Array with specified length:", arr3);
let arr3D = [[1, 2], [3, 4], [5, 6]];